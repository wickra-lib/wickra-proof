# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_proof.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKPROOF_INC)
PKG_LIBS = -L$(WKPROOF_LIB) -l:wickra_proof.dll
